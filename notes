9a9x в sequences и records - пустой файл, так как, скорее всего, не ищется CA
там есть некоторые файлы pdb и cif с одинаковыми названиями, если есть в названии расширение .pdb, то это pdb

в pdb следующих файлах нет SEQRES, поэтому в sequences этих файлов нет последовательности SEQRES, но все они дублируются в mmCIF
8ZLI.pdb_sequences.fa
9A8E.pdb_sequences.fa
9ATW.pdb_sequences.fa
9e8v.pdb_sequences.fa
9e8w.pdb_sequences.fa
9e8x.pdb_sequences.fa
9e8y.pdb_sequences.fa
9JAZ.pdb_sequences.fa
9JB0.pdb_sequences.fa
9JB1.pdb_sequences.fa
9JB2.pdb_sequences.fa
9kc2.pdb_sequences.fa
9kcn.pdb_sequences.fa
9kco.pdb_sequences.fa
9LIV.pdb_sequences.fa
9M5Q.pdb_sequences.fa
9M5R.pdb_sequences.fa
9O4B.pdb_sequences.fa
9UMH.pdb_sequences.fa

для некоторых из оставшихся cif есть файл pdb с таким же названием, где atom seq совпадает, а seqres нет

Для обработки структур алгоритмом APD необходим угол наклона twist для каждой структуры. Где его взять?
Он есть в статьях, в которых описывают структуру. Иногда есть и в pdb-файлах, но под разнымы именами и в различных местах. ???

Также необходимы координаты X, Y для центра масс. В некоторых PDB по нулям, но расчёт через паймол говорит о других данных. ???

Алгоритм местами не справляется: влияет твист, индексация цепей и прочее. Пока без ошибок он попарно сравнил немного. Логи (family_logs) в примере. 
Там число остатков, флипов и два апд (второй с учётом z-смещения)

5w7v странная структура из нескольких цепочек в одном слое. При подсчёте APD берём первую цепь, видимо в её случае она уникальна, поэтому нет общих остатков в логе

2nao 5aef
